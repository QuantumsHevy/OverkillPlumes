// X-2 'Heinlein' Nuclear Salt Water Rocket Engine (fft-nswr-1)
@PART[fft-nswr-1]:NEEDS[FarFutureTechnologies,Waterfall]:FOR[OverkillPlumes]
{
    @MODULE[ModuleWaterfallFX]:HAS[#moduleID[nswrFX]]
    {
        @TEMPLATE:HAS[#templateName[fft-nswr-low-1]]
        {
            // Point at our own template instead of the shared one FFT uses.
            @templateName = overkill-nswr-heinlen

            // Original: 3.25, 3.25, 3
            @scale = 4, 4, 9
        }
    }
}

// This mod is not yet in a usable state!
// Leaving it installed may interfere with, replace, or delete the existing plume(s) in-game.
// If you installed this early, please remove it until a usable state is announced or a release is made.